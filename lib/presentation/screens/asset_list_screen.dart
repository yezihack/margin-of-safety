import 'package:flutter/material.dart';

class AssetListScreen extends StatelessWidget {
  const AssetListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('资产列表'),
      ),
      body: const Center(
        child: Text('Asset List Screen - TODO'),
      ),
    );
  }
}
