import 'package:flutter/material.dart';

class RebalanceScreen extends StatelessWidget {
  const RebalanceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('再平衡'),
      ),
      body: const Center(
        child: Text('Rebalance Screen - TODO'),
      ),
    );
  }
}
